# 웹 프로그래밍

---

## 프론트 필수 기술

### HTML
웹페이지의 구조와 내용을 담당

- **DOM Tree**: HTML 문서를 트리 구조로 표현한 것(Document가 최상위 노드로 존재, 브라우저에서 생성)

### CSS
웹페이지의 디자인과 스타일 담당

- **CSSOM**: CSS를 트리 구조로 표현한 것

### 자바스크립트
웹페이지의 동작과 기능 담당

---

## 렌더링

- **Render Tree**: DOM, CSSOM트리를 생성하고 합쳐서 실제화면에 그릴 요소만 모아놓은 트리

---

## 과거

### 서버사이드 렌더링 (SSR)
- 서버에서 프론트 파일들을 사용해 완성된 화면을 클라이언트에게 전달
- 검색 엔진에 노출이 잘됨(완성된 HTML 파일 덕에 검색엔진이 내용을 바로 읽을 수 있음)

### 멀티 페이지 어플리케이션 (MPA)
- 페이지마다 별도의 HTML 파일 존재
- 페이지를 이동할 때마다 서버에서 새 HTML을 받아옴(매번 새로고침)

---

## 현재

### 클라이언트 사이드 렌더링 (CSR) 또는 CSR+SSR 하이브리드
- 서버는 HTML(내용이 없고 빔)+CSS+자바스크립트 전달 → 클라이언트에서 자바스크립트가 DOM트리 만들고 화면 렌더링
- HTML 코드는 JS안에 포함
- SSR보다 초기 지연시간이 더 걸림(단 이후에 페이지 전환 속도는 빠름)
- 서버 부담이 줄어듬
- 검색 엔진 노출 불리(빈 HTML이라 검색엔진이 내용을 읽을 수 없음)

### 대표 프레임워크

| 방식 | 프레임워크 |
|---|---|
| **CSR** | React, Vue, Angular |
| **SSR+CSR 하이브리드** | Next.js (React 기반), Nuxt.js (Vue 기반) |

### 싱글페이지 어플리케이션 (SPA)
- 서버에서 필요한 데이터를 가지고 와서 렌더링을 클라이언트에서 수행해서 그 부분만 컴포넌트만 바꿔치기 해서 수행
- 페이지 전환이 빠름(이미 JS를 받아놨기 때문에 새로고침 없이 즉시 전환)
- 사용자 경험이 좋음(앱처럼 부드럽게 동작)
- **AJAX**: 페이지를 새로 고치지 않고 서버로부터 필요한 데이터만을 가지고 와서 웹페이지 화면을 동적으로 변경하는 방식
- 핫 리로드(코드 수정 시 페이지 새로고침 없이 변경사항이 바로 반영되는 것)
