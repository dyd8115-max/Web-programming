# 웹프로그래밍

---

## 프로토콜 계층

| 계층 | 이름 | 예시 |
|------|------|------|
| 7계층 | 응용 계층 | HTTP, HTTPS, FTP, DNS, SMTP, 사용자가 직접 사용하는 서비스 |
| 6계층 | 표현 계층 | SSL, TLS, 데이터 형식 변환, 암호화 |
| 5계층 | 세션 계층 | NetBIOS, 통신 연결 시작, 유지, 종료 관리 |
| 4계층 | 전송 계층 | TCP, UDP, 데이터 전송 방식 결정 |
| 3계층 | 네트워크 계층 | IP, ICMP, 목적지까지 경로 결정 |
| 2계층 | 데이터링크 계층 | Ethernet, MAC, 같은 네트워크 내 데이터 전송 |
| 1계층 | 물리 계층 | 케이블, 광섬유, 실제 신호 전송 |

- HTTP는 보통 TCP랑만 엮임 (반대로 TCP는 응용계층과 다양하게 엮임, TCP가 소켓(통로) 역할을 하고 HTTP는 그 통로를 지나는 데이터)
- **TCP**: 연결 보장, 데이터 손실 없음 (느리지만 안전, 순서 보장)
- **UDP**: 연결 보장 없음, 빠름 (순서 바뀔 수 있음, 게임 혹은 실시간 서비스에 사용)
- **패킷**: 네트워크에서 데이터를 전송하는 최소 단위
- **호스트 네임**: 네트워크에 연결된 장치에 부여된 이름 (IP 대신 기억하기 쉬움)
- **패스 네임**: 파일 시스템 내에서 특정 파일이나 디렉터리의 위치를 나타내는 경로

```
https://github.com/LeeYongkun/study
프로토콜://호스트 네임/패스네임
```

---

## 웹서버

**흐름**: 웹서버 시작 → 소켓 생성 → 바인딩 → 리스닝 시작 → 클라이언트 연결 요청 오면 수락

### 주요 개념

- **소켓**: 클라이언트와 서버가 네트워크 통신하는 연결 통로 (양방향 통신 가능)
- **소켓 식별자**: 개발자가 소켓 생성 시 운영체제가 부여하는 번호 (운영체제 내부에서만 사용)
- **IP 주소**: 네트워크에서 컴퓨터를 찾기 위한 고유 주소 (ex: 192.168.0.1)
- **포트 번호**: 컴퓨터 내에서 프로그램을 구별하는 번호 (ex: HTTP → 80, HTTPS → 443, Spring Boot → 8080)
- **프로토콜**: 통신할 때 지켜야 하는 규칙 (ex: TCP, UDP, HTTP 등등, 소켓 생성 시 개발자가 지정)
- **바인딩**: 소켓에 IP주소와 포트번호, 프로토콜을 묶어서 등록하는 것 (네트워크로 외부 통신 시 사용, 개발자 주체)
- **리스닝**: 소켓이 클라이언트 요청이 올 때까지 대기
- **5-튜플**: 출발지 IP, 출발지 포트, 목적지 IP, 목적지 포트, 프로토콜 — 총 5개로 구성된 소켓 연결을 유일하게 식별하는 정보 묶음

### 연결 방식

- **Non-Persistent**: 요청마다 TCP 연결을 새로 만들고 응답 후 바로 끊는 방식 (헤더 `Connection: close`, 비효율적, 리소스를 아끼거나 짧은 연결이 필요할 때 사용)
- **Persistent**: 하나의 TCP 연결로 여러 HTTP 요청/응답을 처리하는 방식 (헤더 `Connection: keep-alive`, 빠름, 효율적)

---

## 전체 흐름

### 1. 서버 준비 단계
- 소켓 생성 (소켓 1)
- 바인딩
- 리스닝 단계로 대기

### 2. 클라이언트 접속
- 사용자가 해당 앱에 접속 시도
- 클라이언트 소켓 생성 (통신 시 양쪽 소켓 필요, 브라우저가 자동 생성, 소켓 2)
- 클라이언트 소켓 생성 당시 5-튜플 할당
- 서버로 연결 요청

### 3. 서버 연결 수락
- 서버에서 연결 요청 확인
- TCP 연결 파이프 생성
- 새 소켓 생성 (소켓 3, 기존 소켓 1은 계속 새로운 클라이언트를 대기해야 하기 때문에 새로 생성, 5-튜플 할당)
- 클라이언트 전용 소켓으로 통신 시작

### 4. 데이터 주고받기
- 클라이언트 소켓 (소켓 2)이 페이지 생성 요청
- 서버 소켓 (소켓 3)이 HTML, CSS, JS 전달
- 클라이언트 컴퓨터에서 프론트 코드로 페이지 생성

### 5. 연결 종료
- 통신이 끝나면 소켓이 닫힘 (소켓 2, 소켓 3, 통신 차단, TCP 파이프 차단)
- 식별자 반환 (운영체제에 번호 돌려줌, 다른 소켓이 번호 재사용 가능)
- 운영체제 테이블에서 삭제 (소켓 삭제)

### 소켓 정리

- 양쪽의 프로토콜, IP주소는 변동 없음, 서버 포트번호는 하나로, 클라이언트 포트 번호(소켓 2)는 프로그램마다 변동
- 소켓 1과 소켓 3(서버 소켓들)은 식별자 번호는 다르나 바인딩은 같음
  - 단, 소켓 1은 기존 서버 바인딩만 / 소켓 3은 기존 서버 바인딩 + 클라이언트 IP, 포트번호가 있는 5-튜플이라는 차이점이 있음

---

## C10K Problem

**C10K (Connection 10K Problem)**: 하나의 서버에서 동시에 10,000개 클라이언트 연결을 처리하는 문제

- **과거**: 클라이언트당 스레드(연결을 처리하는 작업 단위) 1개 생성 → 메모리 부족
- **현재**: NGINX (논블로킹 + 비동기 방식) 또는 Node.js의 싱글 스레드

### 해결 방식

- **NGINX**: 이벤트 드리븐 + 비동기 논블로킹
  - worker 프로세스 몇 개만 사용 (CPU 코어 수만큼) → 각 worker가 수천 개 연결 처리 → worker 1개로 수천 개 스레드 처리 가능
  - 이벤트 루프가 계속 돌면서 기다리지 않고 이벤트 발생할 때마다 처리

- **싱글 스레드**: 방식은 NGINX와 같으나 스레드 1개만 사용 (한 번에 하나씩 일처리 시작, CPU 코어 수에 맞게 싱글 스레드를 각각 실행하는 클러스터 모드도 존재)

- **이벤트 드리븐**: 이벤트가 발생했을 때 그에 맞는 동작을 실행하는 방식 (논블로킹)
- **워커 프로세스**: 실제 요청을 처리하는 일꾼 프로세스
